- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.mackup.cfg:
    ~/.nanorc:
    ~/.config/bat:
      create: true
      path: config/bat
- create:
    - ~/Work
    - ~/Playground

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
    - command: ./setup_zsh.zsh
      stdout: true
      stderr: true
