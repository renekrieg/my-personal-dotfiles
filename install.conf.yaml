- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work
    - ~/Playground

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
