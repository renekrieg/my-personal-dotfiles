- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work
    - ~/Playground

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
